services:
  - type: web
    name: django-backend
    env: python
    buildCommand: "./build.sh"
    startCommand: gunicorn core.wsgi:application
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: myapp-db   # <-- Must match your Render DB name
          property: connectionString
